{
  "private": true,
  "scripts": {
    "build": "npm run build -ws",
    "watch": "npm run watch -ws"
  },
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "@types/node": "^16.4.10",
    "typescript": "^4.3.5",
    "esbuild": "^0.19.9"
  }
}